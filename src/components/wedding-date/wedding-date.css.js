import styled from 'styled-components';

export const Container = styled.div`
  display: block;
  text-align: center;
  color: #5a4a5a;

  p {
    margin: 0;
    padding: 0;
  }

  span {
    display: block;
  }

  .date {
    font-weight: 700;
    font-size: 1.5rem;
  }

  .time {
    font-weight: 700;
    font-size: 1.5rem;
  }

  .reception {
    font-weight: 300;
    font-style: italic;
  }
`;
